# VIGÍA Backend — dependencias Python
# Instalar con: pip install -r requirements.txt

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client async (SODA API)
httpx>=0.27.0

# Analítica masiva — Parquet en RAM (Vast.ai / producción)
pyarrow>=17.0.0
pandas>=2.2.0

# Config
python-dotenv>=1.0.0

# Tipado extendido (Python <3.12)
typing-extensions>=4.12.0
